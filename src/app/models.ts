export type Slip = {
    slip_id: number;
    advice: string;
}

export type ImageData = {
    name: string;
    alt: string;
}

export type Advice = {
    text: string;
    imageName: string;
    alt: string;
}